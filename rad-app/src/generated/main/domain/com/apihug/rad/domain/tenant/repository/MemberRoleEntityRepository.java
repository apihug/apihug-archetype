// @formatter:off
package com.apihug.rad.domain.tenant.repository;

import com.apihug.rad.domain.bootstrap.config.RadJdbcSupport;
import com.apihug.rad.domain.tenant.MemberRoleEntity;
import com.apihug.rad.domain.tenant.dsl.MemberRoleEntityDSL;
import hope.common.spring.data.persistence.CollapseHelper;
import hope.common.spring.data.persistence.spring.HopeJdbc;
import java.lang.Long;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.Map;
import java.util.Optional;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.data.repository.ListCrudRepository;
import org.springframework.stereotype.Repository;
//TRAIT-import
import java.util.List;
import java.util.Collection;
import hope.common.meta.annotation.Template;
import org.springframework.data.jdbc.repository.query.Query;

/**
 * JDBC repository for the MemberRoleEntity entity.
 *
 * GENERATION NOTICE:
 * This interface is AUTOMATICALLY GENERATED by the apihug 'wire' command.
 * DO NOT manually modify this file - any handwritten changes will be OVERWRITTEN
 * during the next compilation/generation cycle.
 *
 * EXTENSION MECHANISM:
 * For customizations, use the dedicated extension interface:
 * {@code t.com.apihug.rad.domain.tenant.repository._MemberRoleEntityRepository}
 *
 * The extension interface (which must also be an interface) supports:
 * 1. Standard Spring Data JDBC custom query methods (following method naming conventions)
 * 2. Custom default methods with implementations
 * 3. Domain-specific query abstractions
 *
 * MERGE PROCESS:
 * During the final steps of the 'wire' task execution, any methods defined in the
 * extension interface are automatically merged into this generated interface.
 * This ensures your customizations are preserved while maintaining the benefits
 * of code generation.
 *
 * IMPORTANT REMINDER:
 * After adding or modifying any methods in the extension repository
 * ({@code _MemberRoleEntityRepository}), you MUST run the 'wire' command to merge
 * your changes into this repository interface. The extension changes will NOT
 * take effect until the wire task is executed.
 *
 * USAGE GUIDELINES:
 * - Inject and use this interface directly in your services
 * - All custom queries should be defined in the extension interface
 * - Leverage the inherited capabilities from parent interfaces for standard CRUD
 * and domain-specific operations
 *
 */
@Repository
@SuppressWarnings("Duplicates")
public interface MemberRoleEntityRepository extends HopeJdbc<MemberRoleEntity>,
    RadJdbcSupport,
    MemberRoleEntityDSL,
    ListCrudRepository<MemberRoleEntity, Long> {

  //==========TRAIT-BEGIN==========


  @Query
  List<MemberRoleEntity> findByMemberId(Long memberId);

  @Query
  List<MemberRoleEntity> findByMemberIdIn(Collection<Long> memberIds);

  @Query
  void deleteByMemberId(Long memberId);

  @Query
  void deleteByMemberIdAndRoleId(Long memberId, Long roleId);

  @Query
  boolean existsByMemberIdAndRoleId(Long memberId, Long roleId);


  //==========TRAIT-END==========

  Logger __MemberRoleEntityLogger = LoggerFactory.getLogger(MemberRoleEntityRepository.class);

  @Override
  default Logger logger() {
    return __MemberRoleEntityLogger;
  }

  default int update(final MemberRoleEntity updated) {
    Optional<MemberRoleEntity> existOptional = findById(updated.getId());
    if (existOptional.isPresent()) {
      MemberRoleEntity exist = existOptional.get();
      return merge(exist, updated);
    }
    return 0;
  }

  default int merge(final MemberRoleEntity exist, final MemberRoleEntity updated) {
    final Map<String, Object> args = new HashMap<>();
    args.put("id", exist.getId());
    args.put("memberId", CollapseHelper.collapse(MemberRoleEntity::getMemberId, updated, exist));
    args.put("roleId", CollapseHelper.collapse(MemberRoleEntity::getRoleId, updated, exist));
    return namedJdbcTemplate().update(_SQL._UPDATE, args);
  }

  /**
   * Those are H.O.P.E. generated SQL, do not modify manually, it will be overwritten every time !
   */
  interface _SQL {
    String _UPDATE = "UPDATE SYS_MEMBER_ROLE SET `MEMBER_ID` = :memberId, `ROLE_ID` = :roleId WHERE `ID` = :id";
  }
}